{
  "private": true,
  "scripts": {
    "build": "pnpm run -r --parallel build",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.1.0"
  },
  "engines": {
    "node": "^18.16.0",
    "pnpm": "^8.4.0"
  }
}
